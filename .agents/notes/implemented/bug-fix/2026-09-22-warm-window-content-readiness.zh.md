# 预热窗口的身份转换与内容就绪判定

Status: implemented
Translation: current
PR: [#885](https://github.com/LodyAI/Lody/pull/885), [#914](https://github.com/LodyAI/Lody/pull/914)

[English](2026-09-22-warm-window-content-readiness.md)

## 摘要

预热窗口领取后会露出 Loading 或暂时性的「会话不存在」，保留备用身份，并在崩溃后回到中性路由。
现在领取时保存真实身份与恢复地址；原生窗口保持隐藏，直到匹配目标的内容绘制完成，不再显示
空白 renderer 遮罩。准备时间仍计入点击到显示耗时。本地窗口复用元数据与已加载会话快照，
持久化及同步游标独立。基准发现的固定 151 ms 未命中等待，已通过在线窗口枚举和明确的缺失
响应去除。本地备用窗口现已在领取前启动工作区初始化，同工作区导航复用该 Runtime。
完整应用点击到显示的耗时尚未验证。

## 决策与证据

本次修正[窗口预热决策](../feature/2026-09-18-desktop-window-prewarming.zh.md)，
最终意图由[桌面窗口 Spec](../../../../specs/desktop-windows.zh.md) 维护。

`window-target.ts` 在导航前清除备用身份并保存真实恢复目标。准备期间暂时关闭后台节流，
让隐藏 renderer 能加载与绘制。主进程仅接受已领取 WebContents 发出的、匹配工作区与会话的
`app.windowContentReady`，随后恢复节流并显示、聚焦原生窗口。renderer 要求连续两帧出现
目标标记；对话标记须等待历史实际加载，不能只依据已知历史条目数。空会话仍需同步完成。
移除旧不透明遮罩：它把 Loading 变成空白窗口，并没有减少工作。主进程独立的五秒期限用于
导航或渲染失败时显示恢复界面，不冒充内容已就绪。关闭窗口取消待显示任务；窗口显示后才
创建替补，避免与目标准备争抢资源。

`RuntimeProvider` 在备用窗口仍处于中性路由时初始化隐式本地工作区，将 Repo 创建、本地元数据
同步和窗口间初始化提前到点击之前，不写入路由上下文，也不挂载会话。领取同一工作区时，
有效工作区标识不变，因此保留已就绪 Runtime 或正在进行的初始化。缺少本地身份时继续等待；
普通中性窗口和云端 Runtime 仍由路由触发初始化。目标历史加载与绘制仍在选择会话后进行。

`window-state.ts` 统一负责产品窗口注册与关闭清理。已领取窗口可以接替主窗口，且不会随原窗口
关闭而销毁。只有当前备用窗口拥有自身外壳就绪计时器，关闭旧的已领取窗口不能取消替补超时。

`doc-meta.ts` 区分初始化扫描与各会话的投影处理完毕。排队事件、最新完整读取及未解决或失败的
读取，只阻止自身会话的缺失判定。删除取消过期等待；Runtime 销毁阻止旧结果写回。否决全工作区
统一等待，避免无关失败让缺失会话永久 Loading。已存在会话不订阅等待状态变化，工作区外壳仍
使用初始化就绪状态。

`local-window-bootstrap.ts` 仅在本地模式通过按工作区隔离的 BroadcastChannel 交换 CRDT 元数据
与已持有的会话快照。元数据异步合并并经过 Repo/Flock 正常投影。每个 Runtime 在其生命周期内
持有独立命名的 Web Lock，枚举锁即可知道在线窗口，无需持久化成员列表或心跳；renderer 退出时
锁自动释放。会话先读磁盘，命中时不要求其他窗口重复导出。未命中时仅向枚举到的窗口请求；
没有窗口立即回退，所有已枚举窗口明确回复缺失后也立即回退。150 ms 期限仅处理枚举后退出或
停止响应的窗口。没有 Web Locks 时不请求窗口会话快照。消息上限 16 MiB；销毁时关闭通道并结束等待。

快照在创建历史读取器之前合并进已有文档，保留未发送编辑。实测否决了「先建读取器，再异步导入」：
虽然不再等待，却让读取投影逐个处理批量导入事件，大幅增加长历史开销。不额外创建 UI store、
Mirror 或守护进程连接，也不共享持久化存储和同步游标。云端与双模式 Runtime 不参与共享。

## 验证与限制

46 项确定性组件测试及九项共享 IPC 测试覆盖身份转换、恢复、发送方与目标匹配、隐藏准备、
超时和关闭清理、替补时序、元数据竞争、CRDT 复用与合并、窗口缺失和缓存选择。components
及 Electron 类型检查通过；Provider 生命周期测试验证路由前准备、已完成与进行中初始化的复用、
范围切换、身份等待与销毁。修改的 helper 与 benchmark lint 无警告或错误。

[基准](../../../../packages/components/benchmarks/window-bootstrap/README.md) 保留原退化数据和
修正后的测量。它使用两个真实 Electron renderer、原生 BroadcastChannel/Web Locks/IndexedDB、
合成 CRDT 历史及生产读取器。原生显示探针运行生产主进程与 renderer 的就绪代码，在首次显示时
截图并确认合成界面已有 30 行可读内容。这验证显示顺序，不代表完整 Lody React 界面的验收。
这些记录早于工作区 Runtime 提前初始化的改动，不能用于测量本次提前初始化的提速幅度。
点击到显示与数据可读耗时不能混为一谈。

完整应用视觉验收尚未完成；E2E 禁用预热池，当前工作树缺少完整桌面／CLI 构建产物及 ACP 子模块。
`pnpm check` 停在 `packages/ignore` 依赖缺失处；`check:public-boundary` 和 `docs check` 报告指向
缺失子模块的既有引用。
