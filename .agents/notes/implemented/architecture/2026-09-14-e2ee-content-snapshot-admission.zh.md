# 内容快照宿主准入（独立包端口）

Status: implemented
Translation: current

[English](./2026-09-14-e2ee-content-snapshot-admission.md)

## 摘要

内容快照的发表许可是宿主在提交时做的准入，不是解密成功、运输 offset、旧 head、自称时间或 `verified=true`。Zixuan 于 2026-09-14 同意：核验当前设备文档写权限并把已认证提交设备绑到签名设备；截止仍是最坏 15 分钟窗口；已接纳字节的内容身份不可被不同内容占用同一位置，完全相同的重试可以幂等；读端仍验签与 Org/文档/epoch/purpose/offset，合法历史在作者后撤后仍可打开。这信任宿主执行，不抵抗恶意宿主与已撤权设备串通，不发明 receipt。独立包提供 `createContentSnapshotPublication` 与 fail-closed 测试宿主；生产 JWT/网关仍未实现。

## 决定与范围

提交时授权与读时历史接受分开。`mayWriteDocument` 只约束诚实 `seal`；恶意客户端绕过它。宿主在同一本地排他操作里：验签（不解密）、提交设备 === 签名设备、当前文档写、注入时钟上的原租约截止、按精确字节记录 `(offset → body)`。原租约在任何 await 之前拷贝；异步验签与当前写权限之后、写入紧前再核同一截止，不重签、不重新起算。`now == expires` 视为过期。同 offset 同字节返回幂等且不把当前指针移回旧位置；同 offset 不同字节拒绝；数值更大的新 offset 可成为当前快照；更早 offset 拒绝。无跨流事务。测试对等体在未提供准入端口时对 snapshot PUT 返回 403。

不改变账本快照 `7d8d553`、增量 update_batch 加密、权限账本背书资格，也不接线 Convex/生产网关。

## 证据与取舍

曾把「宿主返回的 offset 字节」写成发表证据，那不是用户确认项，已撤回。不用当前 `deviceMayWriteDocument` 在 `open` 上拒绝，否则合法历史会在撤权后失效。不用 receipt：未获单独批准，且本阶段明确信任宿主执行。

测试：`test/snapshot-admission.test.ts`（guest/陌生/设备不一致/过期与延迟不续期/验签期间租约到期不得发表/验签期间撤权/排队中改请求对象不延长原截止/同 offset 替换/幂等/撤权后新发表失败且历史仍可 open/无端口 fail-closed/校验失败不推进游标），以及 C1 加密快照 bootstrap 走准入头。生产 JWT 截止未验。独立 review 在 `baea079` 上用真实 Ed25519 验签把时钟推到 `expires` 后仍 accepted，属 P1；修复后不得再只在 `authenticate` 之前检查租约。

## 恢复设备权限修正（2026-09-14）

白皮书对照发现 `deviceMayWriteDocument` 错误放行非 Guest 成员的恢复设备。现明确只允许 personal/machine 类型。回归通过真实签名账本登记 R，绕过诚实客户端 seal 检查，验证宿主拒绝 R 且不保存快照。本次恢复既定权限，不新增信任协议。本阶段回归 93 项通过，包类型检查通过。生产接入与本轮工作区修复的独立复审仍分开。

## 持久化后续（2026-09-15）

阶段 2 以注入的同步事务端口及 Node-only SQLite 后端替代原进程 Map 的权威地位。不同于上方初版，异步验签在数据库锁外；取得锁后重新读取持久身份/当前指针，并重新核对当前写权限与同一原租约。密文字节、接纳身份、当前指针一次提交。显式区分创建与打开，缺失/外来数据库不得静默重建。精确重试跨重启仍幂等且不回退。测试覆盖提交前后真实进程死亡、进程竞争、身份与指针间 SQL 注入失败、事务回滚及过期句柄、撤权后历史解密与取得事务时租约到期。不代表生产 Streams/JWT 接线或分布式存储协议。
