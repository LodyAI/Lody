// The control-plane schema is shared so the CLI can build the same control
// plane as the renderer. See `@lody/shared/session-control-plane`.
export { CONTROL_PLANE_IGNORED_ROOT_KEYS, sessionControlPlaneSchema } from '@lody/shared';
