// The control-plane doc facade is shared so the CLI can build the same control
// plane as the renderer. See `@lody/shared/session-control-plane`.
export { createControlPlaneDoc } from '@lody/shared';
