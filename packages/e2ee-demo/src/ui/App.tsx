export function App() {
  return (
    <p>
      Use the controls above. This React mount is a placeholder; Node tests drive the host through
      public APIs. Isolated browser contexts must not share keys.
    </p>
  );
}
